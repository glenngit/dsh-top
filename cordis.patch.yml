# dsh-top — System monitoring bundle layer.
# Host half registers GET /api/dsh-top-stats; the package also declares a
# dsh.client web bundle that paints the top-right overlay widget.
- insert:
    - id: dsh-top
      name: dsh-top
